{
  "name": "nodebbs",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel --filter=nodebbs-api --filter=nodebbs-web",
    "dev:app": "turbo run dev --parallel --filter=nodebbs-api --filter=nodebbs-app"
  },
  "devDependencies": {
    "turbo": "^2.6.0"
  },
  "packageManager": "pnpm@10.21.0",
  "engines": {
    "node": ">=18"
  },
  "author": "qianshan w",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/aiprojecthub/nodebbs"
  }
}
